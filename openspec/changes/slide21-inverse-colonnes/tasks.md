## 1. Inverser les colonnes de la slide 21

- [ ] 1.1 Frontmatter : `gauche: La machine` / `droite: L'auteur` →
      `gauche: L'auteur` / `droite: La machine`.
- [ ] 1.2 Contenus : intervertir les blocs `::gauche::` et `::droite::` —
      l'auteur (verbatim long, « sauf une, une seule ») passe à gauche, la
      machine (verbatim court, « deux assiettes ») passe à droite.
- [ ] 1.3 Note de présentateur : mettre à jour les repères (gauche =
      l'humain, droite = la machine) et le commentaire de source si présent.

## 2. Vérification

- [ ] 2.1 Slide 21 : auteur à gauche, machine à droite ; les deux verbatims
      restent lisibles, alignement du diptyque correct. Modes clair et sombre.
- [ ] 2.2 Le beat « compter n'est pas lire » tient avec le nouvel ordre de
      lecture.
