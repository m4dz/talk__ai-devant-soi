## 1. Slide 43 — passage en exergue aphorisme

- [ ] 1.1 `slides/pages/08-cloture.md`, 1re slide : frontmatter
      `layout: propos` → `layout: exergue` ; retirer `variant: plain` ;
      garder `noCountdown: true`.
- [ ] 1.2 Contenu : `# C'est mon œuvre.` → `C'est mon œuvre.` (sans `#` — le
      layout exergue rend le texte en blockquote, comme slides 11/18).
- [ ] 1.3 Ne pas toucher aux notes scéniques (`<!-- -->`).

## 2. Vérification

- [ ] 2.1 Slide 43, **clair et sombre** : phrase centrée en `--title-1`, même
      rendu que slides 11 et 18 ; pas de débordement.
- [ ] 2.2 Séquence 42→43→44 : la chute s'ouvre sur l'aphorisme centré, puis la
      slide 44 (declaration) enchaîne sans redite visuelle gênante.
- [ ] 2.3 `openspec validate slide43-exergue-aphorisme --strict`.
