## 1. Retirer la ligne qui révèle la bascule

- [x] 1.1 `ChapterReader.vue` : supprimer le `<p class="reader__hint">…</p>`
      (les deux `<span>`) et la règle CSS `.reader__hint`.

## 2. Réduire le padding

- [x] 2.1 `.reader__text` : `padding: var(--space-md)` → `var(--space-sm)`.

## 3. Vérification

- [x] 3.1 Slide 42 : plus d'indication sous le texte, padding du cadre plus
      serré. Modes clair et sombre.
