## 1. Habiller le cadre en objet pulp

- [x] 1.1 `.reader__text` : retirer `border` et `border-radius` ; ajouter
      `background: var(--pulp-mat)` et `box-shadow: var(--pulp-shadow)`.
      Conserver le `padding`.
- [x] 1.2 Figer le texte pour le mat crème : `.reader__spoken` →
      `color: var(--pal-clair-muted)` ; `.reader__cloned` →
      `color: var(--pal-clair-ink)`.

## 2. Vérification

- [x] 2.1 Slide 42 : le lecteur ressemble à un objet pulp monté (mat crème
      + ombre). Texte lisible sur crème en modes **clair** et **sombre**.
