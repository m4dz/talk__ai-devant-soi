# remote-integration-contract

Jalon 4 (contrat gelé, BLOQUÉ sur la stack) : intégration réelle deck↔machine (POST /generate, GET /chapter, GET /audio, /status optionnel) + protocole de fallback silencieux
