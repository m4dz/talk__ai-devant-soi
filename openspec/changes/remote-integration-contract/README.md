# remote-integration-contract

Jalon 4 (débloqué le 2026-08-07) : intégration réelle deck↔machine (POST /generate, GET /chapter, GET /audio, GET /status sur :8420) + relance unique en fenêtre haute + affichage borné aux marqueurs BASCULE/FIN AUDIO + protocole de fallback silencieux
